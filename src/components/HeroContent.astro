---
import { personalInfo } from '../data/portfolio.js';
import '../styles/components/hero-content.css';
---

<section id="hero" class="hero-section section-card">
    <div class="container hero-content-wrapper">
        <div class="bio-area">
            <h1>Hola, soy <span class="highlight">{personalInfo.name}</span></h1>
            <p class="tagline">{personalInfo.tagline}</p>
            <p class="bio-text">{personalInfo.welcomeMessage}</p>
            <a href="#contact" class="button primary-button">¡Hablemos!</a>
        </div>
    </div>
</section>
